# Handwritten-Digit-Recognition
Project to classify handwritten digits (0–9) from input images using a trained CNN model on the MNIST dataset, allowing users to predict digits from custom images with high accuracy.
